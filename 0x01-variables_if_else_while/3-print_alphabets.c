#include <stdio.h>
/**
 * main - Enters the program to print alphabet in lower then uppercase
 * Return: Always zero
 */
int main(void)
{
	char c = 'a';
	char d = 'A';

	while (c <= 'z')
	{
		putchar(c);
		c++;
	}
	while (d <= 'Z')
	{
		putchar(d);
		d++;
	}
	putchar('\n');
	return (0);
}
