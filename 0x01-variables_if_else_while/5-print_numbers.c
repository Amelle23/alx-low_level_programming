#include <stdio.h>

/**
 * main - enters the program to print numbers from 0 to 9
 * Return: always zero
 */

int main(void)
{
	int n = 0;

	for (n = 0; n < 10; n++)
	{
	printf("%d", n);
	}
	printf("\n");
	return (0);
}
